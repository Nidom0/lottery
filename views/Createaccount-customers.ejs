<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ساخت حساب مشتری</title>
  <link href="https://fonts.googleapis.com/css2?family=Vazir:wght@300;400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <style>
    body {
      margin: 0;
      font-family: "Vazir", sans-serif;
      background: linear-gradient(135deg,#0d0f1b,#101426,#0f162d);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 20px;
    }

    .card {
      width: 100%;
      max-width: 520px;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.15);
      border-radius: 18px;
      padding: 28px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.35);
    }

    h2 {
      margin-top: 0;
      color: #7ec7ff;
      text-align: center;
    }

    label {
      display: block;
      margin-top: 12px;
      margin-bottom: 6px;
      font-weight: 600;
      opacity: .85;
    }

    input {
      width: 100%;
      padding: 12px;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.2);
      background: rgba(255,255,255,0.08);
      color: #fff;
      outline: none;
      font-size: 15px;
    }

    input:focus { border-color: #6ab8ff; }

    button {
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 50px;
      margin-top: 18px;
      background: linear-gradient(135deg,#3a8bff,#6aa7ff);
      color: #0c1228;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 8px 22px rgba(70,140,255,0.35);
    }

    .helper {
      margin-top: 10px;
      text-align: center;
      opacity: .8;
      font-size: 14px;
    }

    .message { margin-top: 12px; padding: 12px; border-radius: 10px; }
    .error { background: rgba(255,98,98,0.18); border:1px solid rgba(255,98,98,0.4); color:#ffc2c2; }
    .success { background: rgba(126,243,255,0.18); border:1px solid rgba(126,243,255,0.4); color:#c3f7ff; }
  </style>
</head>
<body>
  <div class="card">
    <h2>ثبت نام مشتری</h2>
    <p class="helper">برای ساخت حساب، کد ثبت‌نام ارسال‌شده از سمت پشتیبانی و شماره تماس خود را وارد کنید.</p>

    <% if (error) { %>
      <div class="message error"><%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="message success"><%= success %></div>
    <% } %>

    <form method="POST" action="/customer/create-account">
      <label>کد ثبت‌نام</label>
      <input type="text" name="code" required placeholder="مثلاً 458963">

      <label>شماره تماس</label>
      <input type="text" name="phone" required placeholder="09*********">

      <label>رمز عبور</label>
      <input type="password" name="password" required placeholder="رمز دلخواه شما">

      <button type="submit">ورود / ساخت حساب</button>
    </form>
  </div>
</body>
</html>
