<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>بارگذاری مدارک</title>
  <link href="https://fonts.googleapis.com/css2?family=Vazir:wght@300;400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <style>
    body {
      margin: 0;
      font-family: "Vazir", sans-serif;
      background: linear-gradient(135deg,#0d0f1b,#13172b,#0f182f);
      color: #fff;
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding-top: 30px;
    }

    .glass {
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 20px;
      padding: 24px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.35);
      margin-bottom: 22px;
    }

    h2 { margin: 0 0 10px; color:#7ec7ff; }
    p { opacity: .8; }

    label { display:block; margin-top: 14px; margin-bottom: 8px; font-weight: 600; opacity:.85; }

    input[type="file"] {
      width: 100%;
      padding: 12px;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.2);
      background: rgba(255,255,255,0.08);
      color: #fff;
    }

    button {
      padding: 12px 24px;
      border-radius: 50px;
      border: none;
      background: linear-gradient(135deg,#3a8bff,#6aa7ff);
      color: #0d1325;
      font-weight: 700;
      cursor: pointer;
      margin-top: 16px;
      box-shadow: 0 8px 22px rgba(70,140,255,0.35);
    }

    .docs-list { list-style:none; padding:0; }
    .docs-list li { padding: 10px; border-bottom:1px solid rgba(255,255,255,0.08); }

    .success { background: rgba(126,243,255,0.18); border:1px solid rgba(126,243,255,0.4); color:#c3f7ff; padding:12px; border-radius:10px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="glass">
      <h2>سلام <%= customer.fullName %>!</h2>
      <p>در این بخش می‌توانید تصویر پروفایل و مدارک لازم را بارگذاری کنید. پس از ذخیره، تیم پشتیبانی مدارک را بررسی خواهد کرد.</p>
      <a href="/customer/logout" style="color:#ffabab;text-decoration:none;">خروج از حساب</a>
    </div>

    <div class="glass">
      <h3 style="margin-top:0;color:#7ec7ff;">فرم بارگذاری</h3>

      <% if (success) { %>
        <div class="success">اطلاعات با موفقیت ذخیره شد.</div>
      <% } %>

      <form action="/customer/upload" method="POST" enctype="multipart/form-data">
        <label>تصویر پروفایل</label>
        <input type="file" name="avatar" accept="image/*">

        <label>مدارک (می‌توانید چند فایل انتخاب کنید)</label>
        <input type="file" name="documents" multiple>

        <button type="submit">ذخیره اطلاعات</button>
      </form>
    </div>

    <div class="glass">
      <h3 style="margin-top:0;color:#7ec7ff;">مدارک ارسال‌شده</h3>
      <% if (docs.length === 0) { %>
        <p style="opacity:.7;">مدرکی ارسال نشده است.</p>
      <% } else { %>
        <ul class="docs-list">
          <% docs.forEach(doc => { %>
            <li>
              <a href="<%= doc.file %>" target="_blank" style="color:#7ec7ff;">مشاهده فایل</a>
              <span style="opacity:.7;"> — <%= new Date(doc.date).toLocaleString("fa-IR") %></span>
            </li>
          <% }) %>
        </ul>
      <% } %>
    </div>
  </div>
</body>
</html>
