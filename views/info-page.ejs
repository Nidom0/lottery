<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <link rel="stylesheet" href="/public/css/public-chrome.css">
  <style>
    body { background: var(--bg); }
    .hero { display:flex; align-items:center; justify-content:space-between; gap:16px; background: linear-gradient(120deg, rgba(30,115,190,0.08), rgba(58,141,213,0.14)); border:1px solid rgba(30,59,90,0.12); border-radius:18px; padding:18px; margin-top:30px; box-shadow:0 14px 34px rgba(0,0,0,0.08); }
    .hero img { height:64px; background:#fff; border-radius:14px; padding:8px; box-shadow:0 6px 20px rgba(0,0,0,0.08); }
    h1 { margin: 0 0 12px 0; color: var(--primary); }
    p { line-height: 1.9; color:#37465a; }
    .card { background: #fff; border:1px solid rgba(30,59,90,0.08); border-radius:16px; padding:18px; margin-top:14px; box-shadow:0 8px 28px rgba(0,0,0,0.06); }
    .card h3 { color: var(--primary); margin-top:0; }
    a { color: var(--primary); }
  </style>
</head>
<body class="public-body">
  <%- include('partials/public-chrome') %>
  <div class="page-shell">
    <div class="hero">
      <div>
        <h1><%= title %></h1>
        <p><%= subtitle || '' %></p>
      </div>
      <div style="display:flex; gap:10px; align-items:center;">
        <img src="/public/image/Lum.png" alt="lum">
        <img src="/public/image/imam.png" alt="imam">
      </div>
    </div>

    <% sections.forEach(function(sec){ %>
      <div class="card">
        <h3><%= sec.title %></h3>
        <p><%= sec.body %></p>
      </div>
    <% }); %>

    <% if (links && links.length) { %>
      <div class="card">
        <h3>لینک‌های مرتبط</h3>
        <ul>
          <% links.forEach(function(link){ %>
            <li><a href="<%= link.href %>"> <%= link.label %> </a></li>
          <% }); %>
        </ul>
      </div>
    <% } %>
  </div>
</body>
</html>
