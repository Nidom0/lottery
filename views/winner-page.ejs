<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>تبریک! شما برنده شدید</title>

  <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;600;700;900&display=swap" rel="stylesheet">

  <style>
    :root {
      --gold: #ffdf8c;
      --deep: #050811;
      --card: rgba(255, 255, 255, 0.06);
      --blur: blur(18px);
      --midnight: linear-gradient(160deg, #05070f 0%, #0b1228 48%, #060814 100%);
      --royal: linear-gradient(135deg, rgba(255, 222, 138, 0.12), rgba(122, 170, 255, 0.14));
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      padding: 0 16px 120px;
      min-height: 100vh;
      background: radial-gradient(circle at 15% 18%, rgba(255, 222, 138, 0.1), transparent 30%),
                  radial-gradient(circle at 82% 22%, rgba(102, 164, 255, 0.15), transparent 35%),
                  radial-gradient(circle at 45% 80%, rgba(255, 255, 255, 0.08), transparent 26%),
                  var(--midnight);
      font-family: Vazirmatn, sans-serif;
      color: white;
      overflow-x: hidden;
      display: block;
      position: relative;
    }

    .lights {
      position: absolute;
      inset: -10%;
      pointer-events: none;
      background:
        radial-gradient(circle at 10% 20%, rgba(255, 222, 138, 0.16), transparent 30%),
        radial-gradient(circle at 85% 15%, rgba(124, 174, 255, 0.14), transparent 28%),
        radial-gradient(circle at 60% 70%, rgba(255, 255, 255, 0.12), transparent 32%);
      filter: blur(28px);
      opacity: 0.8;
    }

    .wrapper {
      position: relative;
      z-index: 2;
      width: min(960px, 100%);
      text-align: center;
      margin: 100px auto 0;
    }

    .masthead {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 16px;
      margin-bottom: 18px;
    }

    .logo {
      width: 130px;
      height: 60px;
      object-fit: contain;
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 14px;
      padding: 10px 14px;
      filter: drop-shadow(0 0 18px rgba(255,200,20,0.55));
    }

    .divider {
      height: 42px;
      width: 1px;
      background: linear-gradient(180deg, transparent, rgba(255,255,255,0.35), transparent);
    }

    .hero {
      background: var(--royal);
      border: 1px solid rgba(255,255,255,0.18);
      padding: 28px;
      border-radius: 22px;
      backdrop-filter: blur(16px);
      box-shadow: 0 15px 38px rgba(0,0,0,0.65), 0 0 0 1px rgba(255,255,255,0.04);
      position: relative;
      overflow: hidden;
    }

    .heritage {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.12);
      padding: 8px 14px;
      border-radius: 999px;
      font-size: 14px;
      letter-spacing: -0.2px;
      color: #e7f0ff;
      box-shadow: 0 10px 24px rgba(0,0,0,0.3);
      margin-bottom: 12px;
    }

    .heritage .mark {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: linear-gradient(120deg, #ffd97a, #ffb74f);
      box-shadow: 0 0 12px rgba(255, 191, 89, 0.7);
    }

    .heritage strong { color: #ffd97a; font-weight: 800; }

    .hero::before, .hero::after {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
    }

    .hero::before {
      background: radial-gradient(circle at 50% 0%, rgba(255, 210, 120, 0.12), transparent 45%);
      mix-blend-mode: screen;
    }

    .hero::after {
      background: linear-gradient(115deg, rgba(255,255,255,0.04), transparent 55%);
      opacity: 0.9;
    }

    .title {
      font-size: 36px;
      font-weight: 900;
      margin-bottom: 10px;
      color: #8ab8ff;
      letter-spacing: -0.3px;
    }

    .subtitle em {
      color: #ffd76f;
      font-style: normal;
      font-weight: 700;
    }

    .subtitle {
      font-size: 18px;
      opacity: 0.9;
      margin-bottom: 30px;
      line-height: 1.8;
      max-width: 720px;
      margin-inline: auto;
    }

    .envelope-zone {
      display: grid;
      place-items: center;
      margin: 10px 0 18px;
      position: relative;
    }

    .pulse {
      position: absolute;
      width: 230px;
      height: 230px;
      background: radial-gradient(circle, rgba(255, 207, 85, 0.18), transparent 65%);
      border-radius: 50%;
      animation: pulse 3s ease-in-out infinite;
      filter: blur(2px);
      box-shadow: 0 0 0 22px rgba(255,255,255,0.02);
    }

    .envelope {
      width: 210px;
      height: 140px;
      position: relative;
      background: linear-gradient(145deg, #1a203f, #0d1126);
      border-radius: 16px;
      box-shadow: 0 25px 35px rgba(0,0,0,0.45), 0 0 0 1px rgba(255,255,255,0.08);
      cursor: pointer;
      transition: transform 0.5s ease, box-shadow 0.5s ease;
      overflow: hidden;
    }

    .envelope:hover {
      transform: translateY(-6px) scale(1.02);
      box-shadow: 0 28px 44px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,255,255,0.12);
    }

    .envelope .flap {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(180deg, #11162f, #0c1023);
      clip-path: polygon(0 0, 50% 60%, 100% 0, 100% 100%, 0 100%);
      transform-origin: top;
      transition: transform 0.7s ease;
    }

    .envelope .seal {
      position: absolute;
      top: 50%; left: 50%;
      transform: translate(-50%, -50%);
      width: 72px;
      height: 72px;
      background: radial-gradient(circle at 30% 30%, #fff6d9, #fcd775 60%, #e7ae3c);
      border-radius: 50%;
      box-shadow: 0 0 0 6px rgba(0,0,0,0.12), 0 8px 15px rgba(0,0,0,0.35);
      display: grid;
      place-items: center;
      color: #0b0d1c;
      font-weight: 900;
      letter-spacing: -0.5px;
    }

    .envelope .shine {
      position: absolute;
      inset: 0;
      background: linear-gradient(120deg, rgba(255,255,255,0.1), rgba(255,255,255,0));
      mix-blend-mode: screen;
      opacity: 0.7;
    }

    .hint {
      margin-top: 12px;
      opacity: 0.82;
      font-size: 15px;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      color: #dfe8ff;
    }

    .hint .dot {
      width: 10px;
      height: 10px;
      background: var(--gold);
      border-radius: 50%;
      box-shadow: 0 0 12px rgba(255,206,85,0.7);
      animation: pulse 2s infinite;
    }

    .message-layer {
      position: fixed;
      inset: 0;
      display: grid;
      place-items: center;
      padding: 20px;
      background: rgba(5, 8, 17, 0.82);
      backdrop-filter: blur(12px);
      z-index: 10;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.45s ease;
    }

    .message-layer.active {
      opacity: 1;
      pointer-events: auto;
    }

    .message-card {
      width: min(820px, 95vw);
      background: linear-gradient(145deg, rgba(18, 26, 48, 0.92), rgba(8, 12, 25, 0.94));
      border: 1px solid rgba(255,255,255,0.14);
      border-radius: 24px;
      padding: 32px;
      color: #eaf0ff;
      text-align: right;
      box-shadow: 0 20px 45px rgba(0,0,0,0.55);
      transform: translateY(16px) scale(0.98);
      transition: transform 0.45s ease;
      position: relative;
    }

    .letter-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin-bottom: 10px;
    }

    .crest {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.12);
      padding: 8px 12px;
      border-radius: 14px;
      font-weight: 800;
      letter-spacing: -0.2px;
      color: #ffd76f;
    }

    .crest img {
      width: 34px;
      height: 34px;
      object-fit: contain;
      filter: drop-shadow(0 6px 14px rgba(0,0,0,0.4));
      background: rgba(255,255,255,0.04);
      border-radius: 10px;
      padding: 4px;
    }

    .message-layer.active .message-card {
      transform: translateY(0) scale(1);
    }

    .close {
      position: absolute;
      top: 14px;
      left: 14px;
      width: 38px;
      height: 38px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.2);
      background: rgba(255,255,255,0.08);
      color: #fff;
      cursor: pointer;
      backdrop-filter: blur(8px);
      transition: all 0.25s ease;
    }

    .close:hover { background: rgba(255,255,255,0.12); transform: translateY(-1px); }

    .text-content {
      margin-top: 12px;
      line-height: 2.05;
      font-size: 18px;
      color: #eaf0ff;
      white-space: pre-wrap;
      position: relative;
      padding: 16px 18px;
      background: rgba(255,255,255,0.03);
      border-radius: 12px;
      border: 1px dashed rgba(255,255,255,0.08);
    }

    .footer-date {
      margin-top: 26px;
      font-size: 16px;
      opacity: .82;
      border-top: 1px solid rgba(255,255,255,0.12);
      padding-top: 14px;
    }

    .cta {
      margin-top: 24px;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: linear-gradient(120deg, #ffe08f, #ffb647);
      color: #0b0d1c;
      padding: 12px 18px;
      border-radius: 14px;
      font-weight: 800;
      border: none;
      cursor: pointer;
      box-shadow: 0 14px 25px rgba(255,159,67,0.35);
      transition: transform 0.2s ease;
      letter-spacing: -0.2px;
    }

    .cta:hover { transform: translateY(-2px); }

    .confetti-piece {
      position: fixed;
      width: 10px;
      height: 18px;
      top: -20px;
      border-radius: 4px;
      opacity: 0.9;
      z-index: 11;
      transform: rotate(15deg);
    }

    @keyframes pulse {
      0% { transform: scale(0.95); opacity: 0.8; }
      50% { transform: scale(1.05); opacity: 1; }
      100% { transform: scale(0.95); opacity: 0.8; }
    }
  </style>
</head>

  <body>
    <div class="lights"></div>

    <div class="wrapper">
    <div class="masthead" aria-label="نشان افتخار"> 
      <img src="/public/image/Lum.png" class="logo" alt="Lum">
      <div class="divider"></div>
      <img src="/public/image/imam.png" class="logo" alt="Imam">
    </div>

    <div class="hero">
      <div class="heritage"><span class="mark"></span> خط مخصوص مشتریان وفادار <strong>شرکت لوم و امام</strong></div>
      <div class="title">تبریک! 🎉</div>
      <div class="subtitle">
        از سوی <em>مجموعه‌ای با سابقه‌ای درخشان</em> این دعوت طلایی به شما تقدیم می‌شود. لحظه‌ای که منتظرش بودید همین حالاست؛ برای مشاهده پیام اختصاصی، پاکت طلایی را لمس کنید.
      </div>

      <div class="envelope-zone">
        <div class="pulse"></div>
        <div class="envelope" id="envelope" aria-label="باز کردن نامه">
          <div class="flap" id="flap"></div>
          <div class="shine"></div>
          <div class="seal">WIN</div>
        </div>
      </div>

      <div class="hint">
        <span class="dot"></span>
        برای مشاهده پیام برنده شدن، روی پاکت کلیک کنید
      </div>

      <div class="subtitle" style="margin-top: 18px; opacity: 0.78; font-size: 16px;">
        به افتخار شما، اعضای باشگاه برندگان ممتاز، با مهر اصالت شرکت، این لحظه ثبت شده است.
      </div>
    </div>
  </div>

  <div class="message-layer" id="messageLayer" aria-hidden="true">
    <div class="message-card">
      <div class="letter-header">
        <div class="crest">
          <img src="/public/image/Lum.png" alt="Lum">
          <img src="/public/image/imam.png" alt="Imam">
          <span>نامه مهر اصالت</span>
        </div>
        <div class="subtitle" style="margin: 0; font-size: 14px; opacity: 0.85;">مخصوص برنده‌ای باورنکردنی مثل شما</div>
      </div>
      <button class="close" id="closeMessage" aria-label="بستن">×</button>
      <div class="title" style="margin-bottom: 4px;">نامه طلایی شما</div>
      <div class="subtitle" style="margin-bottom: 12px;">با افتخار این پیام ویژه را برای شما می‌خوانیم:</div>

      <div class="text-content">
        <%= text %>
      </div>

      <div class="footer-date">
        <strong><%= companyName %></strong>
        <br>
        <%= date %>
      </div>

      <button class="cta" id="readAgain">🎊 دوباره احساسش کن</button>
    </div>
  </div>

  <script>
    const envelope = document.getElementById('envelope');
    const flap = document.getElementById('flap');
    const messageLayer = document.getElementById('messageLayer');
    const closeMessage = document.getElementById('closeMessage');
    const readAgain = document.getElementById('readAgain');

    const colors = ['#ffce55', '#6ae8ff', '#ff6f91', '#7cffb2', '#b48bff'];

    function openEnvelope() {
      flap.style.transform = 'rotateX(-160deg)';
      messageLayer.classList.add('active');
      messageLayer.setAttribute('aria-hidden', 'false');
      sprinkleConfetti();
    }

    function closeEnvelope() {
      flap.style.transform = 'rotateX(0deg)';
      messageLayer.classList.remove('active');
      messageLayer.setAttribute('aria-hidden', 'true');
    }

    function sprinkleConfetti() {
      for (let i = 0; i < 80; i++) {
        const piece = document.createElement('div');
        piece.className = 'confetti-piece';
        piece.style.left = Math.random() * 100 + 'vw';
        piece.style.background = colors[Math.floor(Math.random() * colors.length)];
        piece.style.animation = `drop ${(Math.random() * 1.5 + 1.8).toFixed(2)}s ease-out forwards`;
        piece.style.transform = `rotate(${Math.random() * 360}deg)`;
        document.body.appendChild(piece);

        const removeAfter = (Math.random() * 1200) + 1800;
        setTimeout(() => piece.remove(), removeAfter);
      }
    }

    const style = document.createElement('style');
    style.innerHTML = `
      @keyframes drop {
        to { transform: translateY(110vh) rotate(360deg); opacity: 0; }
      }
    `;
    document.head.appendChild(style);

    envelope.addEventListener('click', openEnvelope);
    closeMessage.addEventListener('click', closeEnvelope);
    readAgain.addEventListener('click', sprinkleConfetti);
    messageLayer.addEventListener('click', (e) => {
      if (e.target === messageLayer) closeEnvelope();
    });
  </script>
</body>
</html>
